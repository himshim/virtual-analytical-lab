name: Deploy Virtual Analytical Lab to GitHub Pages

on:
  push:
    branches: ["main"]   # 👈 Automatically trigger on every push to main
  workflow_dispatch:     # Still keep manual trigger for testing

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload site files
        uses: actions/upload-pages-artifact@v3
        with:
          path: .   # Deploy entire repo

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
